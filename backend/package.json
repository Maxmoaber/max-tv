{
  "_comment": "BACKEND — Servidor Node.js + Express. Este archivo lista las dependencias del backend y los scripts disponibles. npm lo usa para instalar los paquetes necesarios (npm install) y ejecutar comandos (npm run dev, npm start).",
  "name": "movies-backend",
  "version": "0.1.0",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon --watch src --exec node src/server.js",
    "start": "node src/server.js",
    "prisma": "prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "axios": "^1.4.0",
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "node-cache": "^5.1.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "prisma": "^5.0.0"
  }
}
